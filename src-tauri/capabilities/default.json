{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (Phase 1: core only, no plugins)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
