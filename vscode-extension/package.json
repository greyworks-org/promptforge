{
  "name": "promptforge-vscode",
  "displayName": "PromptForge Session",
  "description": "PromptForge session status and explicitly confirmed controls for VS Code.",
  "version": "0.1.0",
  "publisher": "promptforge",
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onView:promptforge.sessionStatus", "onCommand:promptforge.connect", "onCommand:promptforge.handoff"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "promptforge.connect",
        "title": "PromptForge: Connect Session"
      },
      {
        "command": "promptforge.refresh",
        "title": "PromptForge: Refresh Session"
      },
      {
        "command": "promptforge.startSession",
        "title": "PromptForge: Start Session"
      },
      {
        "command": "promptforge.resumeSession",
        "title": "PromptForge: Resume Session"
      },
      {
        "command": "promptforge.addCheckpointNote",
        "title": "PromptForge: Add Checkpoint Note"
      },
      {
        "command": "promptforge.handoff",
        "title": "PromptForge: Handoff"
      }
    ],
    "configuration": {
      "title": "PromptForge",
      "properties": {
        "promptforge.connection": {
          "type": "string",
          "default": "",
          "description": "Session connection copied from PromptForge Local. Keep this local-only value private."
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "promptforge",
          "title": "PromptForge",
          "icon": "resources/promptforge.svg"
        }
      ]
    },
    "views": {
      "promptforge": [
        {
          "id": "promptforge.sessionStatus",
          "name": "Session Status"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
